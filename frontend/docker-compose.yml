version: '3.8'

services:
  nexserve-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    environment:
      - NODE_ENV=production
      - VITE_API_BASE_URL=https://api.nexserve.com
    restart: always

  postgres-db:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: nexserve_admin
      POSTGRES_PASSWORD: nexserve_secure_password_2026
      POSTGRES_DB: nexserve_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis-cache:
    image: redis:7-alpine
    ports:
      - "6379:6379"

volumes:
  postgres_data:
